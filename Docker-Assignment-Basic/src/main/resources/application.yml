server:
  port: 8080

spring:
  application:
    name: Docker-Assignment-Basic
  datasource:
    url: jdbc:mysql://localhost:3306/dockerAssignment
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: redis_cache
      port: 6379